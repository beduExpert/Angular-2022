import { ToLowerCaseDirective } from './to-lower-case.directive';

describe('ToLowerCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ToLowerCaseDirective();
    expect(directive).toBeTruthy();
  });
});
